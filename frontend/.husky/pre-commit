#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npx lint-staged
For your React TypeScript project with accessibility concerns, here are the recommended selections:
What do you want to lint? Select all that apply (JavaScript, TypeScript, JSON, etc.)
What type of modules? → "JavaScript modules (import/export)"
Which framework? → "React"
Does your project use TypeScript? → "Yes"
Where does your code run? → "Browser"
What format do you want your config file to be in? → "JavaScript"
When asked about additional features, select:
Accessibility (jsx-a11y)
Import/export validation
Formatting/Prettier
After the setup completes, I can h